apiVersion: v1
kind: Pod
metadata:
  name: my-pod
  labels:
    app: my-app
spec:
  containers:
    - name: my-container
      image: jenkins/jenkins
      ports:
        - containerPort: 8080   
          protocol: TCP
  
